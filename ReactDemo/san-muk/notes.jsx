//.reduce() in js: is reduces the whole array to one value, the value can be a array, object, string, int, etc
// const output = ARRAY.reduce((accumulator,current value)=>{
//                  return acc + current_value
//                },initial_value);


//and the callback runs for the all the elements in the array
//if we provide the initial value then:
//accumlator will be the initial value and current will be the element at index 0


// if we dont provide the intial value then - the element at index 0 will be the intial value and the current
//value will begin from the index 1 (if the array is empty - and you dont specify an initial value than it will through an error)
//*Always provide an initial value unless you're sure the array is non-empty.


