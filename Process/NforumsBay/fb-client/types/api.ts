export interface API_BULK_DATA {
    boardCategories: {
        name: string,
        boards: object,
        maxNumber: number
    }
}

// <string | null |> this means thar string and null are allowed

